name: clean_architecture_posts_app
description: " simple posts app to practise clean architecture"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  dio: ^5.7.0
  internet_connection_checker: ^3.0.1
  shared_preferences: ^2.3.4
  flutter_bloc: ^8.1.6
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true